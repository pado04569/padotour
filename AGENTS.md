<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# 여행의파도 블로그 글 작성 규칙

블로그/밴드 글 초안을 요청받으면, 먼저 아래 규칙 파일을 읽고 그 규칙을 따를 것:
`C:\Users\Lenovo\Desktop\jianne\여행의파도-컨설팅\분석\SERP분석\블로그_글쓰기_규칙_요약_핸드오프.md`

핵심 요약(전체는 위 파일):
- 글 종류를 먼저 2트랙으로 구분 — (A) 간판 SEO 글(가격 노출) vs (B) 감성글(가격 금지·전화유도).
- 첫 문단 인사말 금지, 검색 질문의 완결형 답변(직항시간·이동·가격·출발조건)을 즉시 배치.
- 한 문장=한 사실, 숫자·고유명사(편명·홀·파·야드·분수) 밀도 최대. 감성 문장은 이미지 캡션으로.
- (A)의 가격: 텍스트(매번 변주) + 유니크 요금표 이미지 병행. 이미지에만 넣기 금지, 같은 이미지 재사용 금지(유사문서).
- 상품·골프장 데이터는 `src/data/tours.json`, `src/data/courses.json` 참조. 전화 010-5301-5250(부산은 "여행의파도 부산").

# ⭐ 상품 제목은 블로그 제목과 절대 같게 쓰지 않는다

**같은 상품을 블로그와 홈페이지에 올릴 때 제목이 같으면 유사문서로 묶인다.**
블로그가 주력 채널이므로, 묶이면 홈페이지 쪽이 밀린다.

## 홈페이지 제목 공식 (사장님 확정 2026-09-10)

```
[출발지] 국가 + 지역 + 골프여행 │ 골프장명 + 박수 │ 그 밖의 정보
```

예)
- `[부산출발] 태국 방콕 골프여행 로터스밸리CC 3박5일 499,000원부터`
- `[부산출발] 태국 방콕 썬밸리 골프여행 무제한 2색골프 3박5일 한식뷔페 무제한 3식`

- **국가와 지역을 모두 넣는다** — `태국 방콕` (지역만 쓰면 국가 검색어를 놓친다)
- **"골프여행"** 을 넣어 검색어를 잡는다
- 그다음 **골프장명 + 박수**, 마지막에 **요금이나 특징**
- 블로그는 "108홀·무제한" 같은 상품 특징으로 제목을 잡으므로 자연스럽게 갈린다

**이 순서를 지키면 화면에서 제목이 세 줄로 알아서 나뉜다.**
상세 페이지는 `골프여행` 뒤와 `N박N일` 뒤에서 줄을 나눈다(`tours/[id]/page.tsx`의 `titleLines`).
줄바꿈은 화면에만 적용되고 **검색용 제목은 한 줄 그대로** 나간다.

> ⚠️ 사고 이력(2026-09-09): 블로그 초안에서 상품을 만들면서 제목을 그대로 옮겨
> `[부산출발] 방콕 로터스밸리CC 골프텔 3박5일 주중 최대 108홀` 로 등록했다. 블로그 제목과 동일했다.

**요금이 바뀌면 제목도 같이 고친다.** 제목에 요금이 들어 있으므로 요금만 고치면 어긋난다.

# ⭐ 화면 표기 규칙 (2026-09-10 정리)

**고객이 직접 읽는 화면이므로 가독성이 우선이다.** 아래는 상품 상세(`tours/[id]/page.tsx`)에 이미 구현되어 있다.
데이터를 넣을 때 이 동작을 알고 넣어야 화면이 깨지지 않는다.

## 줄바꿈은 글자수가 아니라 의미로 나눈다

골프장 이름이 길 수 있어 **길이 기준은 쓰지 않는다**(사장님 지적 2026-09-10).

| 대상 | 나누는 지점 |
|---|---|
| 제목 | `골프여행` 뒤 → `N박N일` 뒤 (`｜`가 있으면 그 경계 우선) |
| 상품 구성(`productSummary`) | 문장(`다.`) · `~하며,` `~하고,` 같은 절 경계 · 줄바꿈(`\n`) |
| 숙소·골프장 설명 | 문장(`다.`) 마다 — `ReadableText`의 `Sentences` |
| 일정(`schedule.desc`) | 이동 화살표(`→`) 마다 — `ReadableText`의 `Steps` |
| 핵심 정보 카드(`holes` 등) | 줄바꿈(`\n`) · ` / ` · 기간의 ` ~ ` 앞 |
| 상품 특징(`highlights`) | 줄바꿈(`\n`) |

- **데이터에 `\n`을 넣으면 그 자리에서 나뉜다.** 조건이 갈리는 항목(홀수·요금)은 직접 넣어준다.
  예) `holes`: `"9/28까지 출발\n18홀 x 3회(54홀)\n10월 이후 출발 주중 36홀\n주말 18홀"`
- `36~54홀` 처럼 **공백 없는 물결표는 나누지 않는다**(숫자 범위). 기간은 항상 ` ~ `(양옆 공백)로 적는다.
- 모든 본문에 `break-keep` 이 걸려 있어 **한글 단어 중간에서는 줄이 끊기지 않는다.** 억지로 `\n`을 넣지 않아도 된다.
- 핵심 정보 카드는 **내용이 길면 글자가 자동으로 작아진다**(24자·45자 기준). 손댈 필요 없다.

## 데이터 표기 주의

- **`minPeople`** — 숫자(`2`)로 넣어도, 문자열(`"4인 (2인 출발 시 송영비 추가)"`)로 넣어도 된다.
  화면에서 `2인 이상` / `4인 이상` + 다음 줄 `(2인 출발 시 송영비 추가)` 로 알아서 정리된다.
  **`"2인 이상"` 처럼 "이상"까지 넣지 않는다** — 중복된다.
- **`subtitle`** — 화면에 쓰지 않는다. 상품 구성·숙소 섹션과 중복이었다. 검색용 설명 예비값으로만 남긴다.
- **`cancelPolicy`** — 취소·환불 규정은 `data/cancelPolicy.ts`의 표준을 전 상품 공통으로 쓴다.
  이 필드에 남은 상품별 안내사항(추가요금·싱글룸·차량 조건 등)은 "이 상품의 추가 안내"로 따로 보인다.
  **여기에 위약금 계단을 새로 넣지 않는다.**
- **`badge`** — 상세 페이지 히어로에는 표시하지 않는다(제목에 이미 `[부산출발]`이 있다). 목록 카드에는 나온다.

## 배치 (사장님 확정)

- 상세 페이지 순서: … 일정 → **예약 문의 → 취소·환불 규정** → 뒤로가기
  **취소규정을 문의 위에 두지 않는다** — 알아보는 단계 고객에게 부담을 준다.
- 히어로 제목은 **가운데 정렬**, 모바일은 세로 가운데.
- 사진 위에 **어둡게 덧씌우지 않는다.** 글자가 조금 덜 보여도 사진이 우선.
- 뒤로가기는 3단계: 상세 `← OO 상품 목록으로` / 나라별 목록 `← 전체 상품 목록으로` / 전체 목록 `← 메인 화면으로`

# ⭐ 상품 등록 전에 인계 문서부터 읽는다 (블로그 → 홈페이지)

새 상품을 올릴 때는 **`_인계/<상품 id>/인계.md`를 먼저 읽고 시작한다.**
블로그 글을 쓴 세션(`Pado blog`)이 랜드사 원본을 정리해 넘겨둔 문서다.

```
_인계/<상품 id>/
  ├ _원본/     ← 랜드사가 준 포스터·요금표·일정표 원본
  └ 인계.md    ← 원본의 숫자를 텍스트로 옮긴 문서
```

## 규칙

**① `_인계/`는 절대 커밋하지 않는다.**
`.gitignore`에 등록되어 있다. **padotour는 공개 저장소**라서 랜드사 요금 원본이
커밋되면 그대로 공개된다. `git add .` 할 때 딸려 들어가지 않았는지 확인한다.

**② `인계.md`가 없으면 만들어 달라고 요청한다.**
없는 채로 추측해서 등록하지 않는다. `_원본/` 이미지가 있으면 열어서 직접 확인한다.

**③ 자료가 부족하면 등록을 멈추고 사장님께 요청한다.**
"없더라고요"라고 보고만 하고 넘어가지 않는다. **무엇이 없어서 무엇을 못 하는지**
구체적으로 말하고 받은 뒤 채운다.
> 사고 이력(2026-09-09): 로터스밸리 상품을 **출발일별 요금표 없이 등록**했다.
> 원본(`land-02.png`)에 전 기간 요금이 있었는데 인계에서 끊겨 확인하지 못했다.

**④ 출발일에 따라 갈리는 항목은 기간별로 나눠 넣는다.**
요금 · 라운딩 홀수 · 식사 포함 여부 · 항공 편명은 출발일마다 다른 경우가 많다.
랜드사 자료 여러 장에 흩어져 있어 **한 장만 보면 틀린다.**

**⑤ `departurePrices`는 비워두지 않는다.**
`인계.md`의 요금표를 날짜로 펼쳐 넣되, **항공 제외일은 빼고** 스팟특가일은 기간요금보다 우선한다.
형식: `[{ "date": "2026-09-20", "price": 499000 }, ...]`

**⑥ 홈페이지 사진은 원본에서 새로 만든다.**
블로그용 가공본(`pado-blog/posts/<폴더>/_ready/`)을 그대로 쓰지 않는다. 유사문서를 피하기 위해
블로그와 다른 사진·다른 문장을 쓴다.

**⑦ 쓰면 안 되는 것** — 취소규정·위약금(예약금 인보이스로 안내) / 업계 용어(GV2·파이널·랜드사·인아웃) /
블로그 본문 문장 그대로 복사.

## 등록 전 확인

- [ ] `_인계/<상품 id>/인계.md`를 읽었는가
- [ ] **`departurePrices`를 채웠는가** (항공 제외일 반영)
- [ ] 홀수·식사·편명이 **출발일 기준으로 나뉘어** 들어갔는가
- [ ] 골프장 페이지가 없으면 함께 만들었는가 (아래 규칙)
- [ ] `_인계/`가 커밋에 포함되지 않았는가

# 상품 등록 시 골프장 페이지 함께 만들기 (기본 동작)

`src/data/tours.json`에 새 상품을 추가하면, **요청이 없어도 골프장 처리까지 함께 한다.**
이유: 골프장 페이지는 상품과 별개로 "골프장 이름" 검색을 잡는 유입구이고, 상품이 끝나도 남는 자산이다.

## 절차
1. 새 상품의 `golfCourse` / `courses` 필드에서 골프장을 확인한다.
2. `src/data/courses.json`에 **이미 있는 골프장**이면 → 그 항목의 `relatedTourIds`에 새 상품 `id`만 추가한다. (새로 만들지 않음)
3. **없는 골프장**이면 → `courses.json`에 새 항목을 추가한다.

## 새 항목 형식
타입 정의는 `src/data/courses.ts`의 `Course`. 기존 16개 항목과 형식을 반드시 맞춘다.

```
{ slug, name, nameEn?, region, country, countryCode,
  summary?, specs?, description, images, hashtags?, relatedTourIds }
```

**필수**
- `slug`: 영문 소문자+하이픈 (예: `kedoin-cc`). 기존 slug와 중복 금지.
- `name`: 한글 골프장명 (예: `케도인CC`). 상품에 쓰인 표기를 따른다.
- `region` / `country` / `countryCode`: 상품 값 그대로 사용
- `description`: 3문단 내외, 문단 구분은 `\n\n`.
  - 1문단 = 코스 정체성(홀 구성·파·전장·설계자·개장). 2문단 = 코스 특징·지형·조망. 3문단 = 여행의파도 상품에서 어떻게 도는지(라운드 수·포함 사항·숙소·직항).
  - 상품에 적힌 사실 + 아래 "코스 사실 조사"로 **검증된 사실만** 쓴다. **추측·미사여구로 채우지 않는다.**
  - 여러 골프장이 한 상품에 묶여 있어도 **골프장마다 문장을 다르게 쓴다.** 같은 문단 복사 금지(유사문서).
- `images`: 4~6장. **콜라주·배너·텍스트가 얹힌 이미지는 제외**하고 실제 코스 사진만. 코스 사진(`course-*.jpg`)이 부족하면 호텔·온천 사진으로 보충한다.
  - 여러 골프장이 이미지 폴더 하나를 공유하면 **대표 사진(첫 장)은 골프장마다 다르게** 배치한다. 그 골프장을 특정할 수 있는 사진(클럽하우스·간판)이 있으면 그것을 첫 장으로.
  - **쓸 수 있는 실제 사진이 4장 미만이면 사장님께 사진을 다시 요청한다.** 억지로 채우지 않고, 콜라주를 끼워 넣지도 않는다. 요청할 때는 **골프장 이름과 지금 몇 장이 있는지**를 함께 알린다. (예: "가산CC는 콜라주를 빼면 실제 코스 사진이 3장뿐입니다. 2~3장 더 주시면 채워 넣겠습니다.")
  - 사진을 받기 전이라도 상품 등록과 골프장 항목 생성은 **가진 사진으로 진행**하고, 부족한 골프장 목록을 작업 보고에 남긴다. 상품 등록이 사진 때문에 멈추면 안 된다.
- `relatedTourIds`: 그 골프장이 포함된 상품 `id` 전부.

**선택 (있으면 반드시 채운다 — 검색 유입과 직결)**
- `nameEn`: 영문·현지 표기 (예: `Mae Jo Golf Club & Resort`, `祁答院ゴルフ倶楽部`). 옛 이름이 있으면 병기(예: `Forest Nankan Golf Club (구 中九州CC)`).
- `summary`: 한 줄 요약. 목록 카드와 meta description에 쓰인다. 숫자를 넣는다. (예: `파72 7,090야드 토너먼트 코스, 리조트에서 걸어서 첫 홀까지`)
- `specs`: `{ label, value }` 배열, 3~6개. 라벨은 기존 항목과 통일 — `코스 구성` / `파 / 전장` / `개장` / `설계` / `코스 특징` / `소재지` / `접근`. **모르는 항목은 넣지 않는다.**
- `hashtags`: 6~8개, `#` 없이 저장. 골프장명 + 지역골프장 + 지역골프여행 + 국가골프여행 + 특징 키워드 조합.

## 코스 사실 조사 (신규 항목을 만들 때)
상품 데이터에 홀 수·파·전장·설계자가 없으면 **웹 검색으로 확인**한다.
- 일본: `GDO` / `楽天GORA` / `じゃらんゴルフ`에서 일본어 정식 명칭으로 검색
- 태국·동남아: `golfscape`, `golfpass`, 코스 공식 사이트
- 확인된 사실만 `specs`·`description`에 넣는다. **이름이 비슷하다는 이유로 다른 골프장 정보를 가져다 쓰지 않는다.**
- 끝내 확인이 안 되면 그 항목은 비우고, 상품에 적힌 사실(라운드 방식·이동시간·포함 사항)만으로 작성한다. 지어내지 않는다.

## 확인
- 상세 페이지(`/courses/{slug}`)와 사이트맵은 `courses.json` 기준으로 자동 생성되므로 추가 작업 불필요.
- 등록 전 점검: 이미지 경로가 `public/` 아래 실제로 있는지, `relatedTourIds`가 `tours.json`에 존재하는지, slug 중복이 없는지.
- 빌드 성공 확인 후 **상품 작업과 같은 커밋**에 포함한다.
- 작업을 마치면 **사진이 부족한 골프장과 필요한 장수**를 보고에 정리해 사장님께 요청한다. 부족한 곳이 없으면 언급하지 않는다.
